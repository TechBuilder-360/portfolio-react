import React from 'react';
import Layout from '../container/Layout/Layout';

const NotFound = () => {
    return (
        <Layout>
            404 Page not found
        </Layout>
    );
};

export default NotFound;