nMight need to add dependency, if using post request doesn't workout well
"graphql": "^15.3.0",
"apollo-boost": "^0.4.9",
"react-apollo": "^3.1.5"

1. Limit social to 3 per user

2. Generate Username from frontend, combine lastname + firstname + 8 uuid characters

3. Store refresh and auth token in local storage for authntication purpose, and add retry for token, 
if valid show dashboard else try using refresh token to obtain new auth token, then retry else display login page.

4. Use react-tag-input for adding skill



Edit Page State
1. Education add, delete, and edit redux works. All that remains is sending action to backend.
2. Experience not working